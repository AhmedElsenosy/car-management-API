# Environment Variables Configuration for Car Management API
# Copy this file to .env and update with your actual values

# Django Secret Key - Generate a new one for production!
# You can generate one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# Debug Mode - Set to False for production/network deployment
DEBUG=False

# Allowed Hosts - Add your server's IP address(es) and domain names
# Example: localhost,127.0.0.1,192.168.1.100,myserver.com
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# CORS Settings - Allow all origins (useful for local network access)
# Set to True if you want to allow requests from any origin
# WARNING: Only use this for development or trusted networks
CORS_ALLOW_ALL=False

# Database Configuration (optional)
# Uncomment and configure if using PostgreSQL instead of SQLite
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
